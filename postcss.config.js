module.exports = {
  plugins: {
    "@tailwindcss/postcss": {},
    autoprefixer: {},
  },
};
// This configuration file sets up PostCSS with Tailwind CSS and Autoprefixer.
// It allows for the use of Tailwind's utility-first CSS framework and ensures that
// CSS is compatible with a wide range of browsers by adding vendor prefixes where necessary.
